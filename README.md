# Bungo Novel Assets

This repository hosts the dynamic asset packs (PNG images + work JSON) for the [文豪ノベル](https://github.com/yuuzimario/bungo-novel) app. Each release tag holds one ZIP per work; the app fetches them on demand based on `src/data/manifest.json`.
